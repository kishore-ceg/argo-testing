apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-demo
  namespace: argocd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
    spec:
      containers:
        - name: kafka-integrator
          image: 440953937617.dkr.ecr.us-east-2.amazonaws.com/kafka-integrator:latest
          command: ["java","-Dspring.profiles.active=dev"]
          ports:
            - containerPort: 3000
